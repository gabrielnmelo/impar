{
  "name": "impar",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply impar-cms --local",
    "migrate:remote": "wrangler d1 migrations apply impar-cms --remote"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
